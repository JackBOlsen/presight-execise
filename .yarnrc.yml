# Use a real node_modules tree rather than Yarn's default Plug'n'Play resolver.
# PnP breaks native modules (better-sqlite3) and needs bundler-specific shims;
# node_modules keeps Docker builds and tooling completely conventional.
nodeLinker: node-modules

enableGlobalCache: true
